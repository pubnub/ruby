# Toplevel Pubnub module.
module Pubnub
  # Every error message from server is formatted into easy to use ErrorEnvelope
  # object, it behaves like normal Envelope.
  class ErrorEnvelope < Envelope
  end
end
